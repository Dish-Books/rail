apiVersion: v1
kind: Service
metadata:
  name: {{ include "rail.fullname" . }}
  labels:
    {{- include "rail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "rail.selectorLabels" . | nindent 4 }}
